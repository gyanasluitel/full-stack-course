const TsIsJs = () => {
    return (
        <>
            <h2 className="title">3. TypeScript is JavaScript</h2>

            <p>Important rule: Every valid JS file is valid TS - TypeScript compiles down to JavaScript - Browsers <span className="stress">do not run TypeScript directly</span></p>
        </>
    )
};

export default TsIsJs;